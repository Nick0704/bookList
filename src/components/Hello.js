import React, {Component} from 'react';
import RaisedButton from 'material-ui/RaisedButton';

const style = {
  backgroundColor: 'red',
};

const Hello = () => (
  <RaisedButton label="Default" backgroundColor="red"/>
);

export default Hello;